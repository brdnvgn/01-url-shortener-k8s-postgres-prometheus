# Connexion PostgreSQL utilisée par l'API
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/url_shortener

# Longueur des codes courts générés
CODE_LENGTH=6

# Taille du pool de connexions asyncpg
POOL_MIN_SIZE=1
POOL_MAX_SIZE=10
